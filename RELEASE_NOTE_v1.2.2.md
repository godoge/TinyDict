# TinyDict v1.2.2 更新日志

> 如果你正在使用 v1.2.1：该版本存在启动问题，请直接升级到 v1.2.2。

## ✨ 新增
- **界面多语言**：新增英文界面，默认仍为简体中文。在「设置 → 语言」中切换，重启程序后生效。
- **生词本导入 / 导出**：支持 txt、csv、json 三种格式，方便备份生词、跨设备迁移，或从备份恢复。

## 🔧 优化
- **顶栏布局调整**：「词库 / 生词本 / 历史 / 更多」移动到窗口右侧，左侧保留后退 / 前进 / 收藏，操作更顺手。
- **生词本分组**：移除「未分组」分类，每个生词至少归属一个分组，分组管理更清晰。
- **启动更稳健**：数据库遇到损坏或文件被占用时，程序会自动尝试恢复，不再卡在启动画面打不开。

## 🐛 修复
- **切换语言后无法启动**：修复在「设置」中切换语言并重启后，程序报错打不开的问题。
- **生词本显示异常**：修复进入生词本后单词列表为空、需要手动点击某个分组才显示的 bug。
